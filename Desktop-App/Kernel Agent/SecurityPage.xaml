<Page
    x:Class="Kernel_Agent.SecurityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kernel_Agent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="30">
        <StackPanel Spacing="25" MaxWidth="900" HorizontalAlignment="Left">

            <StackPanel Spacing="5" Margin="0,0,0,10">
                <TextBlock Text="SECURITY &amp; PRIVACY VAULT" FontSize="24" FontWeight="Bold" Foreground="{StaticResource GoogleRed}"/>
                <TextBlock Text="Configure the ethical and operational boundaries of your agent." Opacity="0.6" FontSize="14"/>
            </StackPanel>

            <Grid ColumnDefinitions="*, *" ColumnSpacing="20">

                <StackPanel Spacing="20">
                    <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" 
                            CornerRadius="12" Padding="20" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE72E;" Foreground="{StaticResource GoogleRed}"/>
                                <TextBlock Text="Application Blacklist" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <TextBlock Text="The agent will immediately terminate its session if these windows become active." FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>

                            <ListView x:Name="RestrictedAppsList" SelectionMode="None" Background="Transparent">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*, Auto" Width="350">
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                                            <ToggleSwitch Grid.Column="1" IsOn="{Binding IsEnabled}" OnContent="Protected" OffContent="Open"
                                                          Toggled="RestrictedAppToggle_Toggled" Tag="{Binding Id}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <Button Content="Add Restricted Application" HorizontalAlignment="Stretch" Padding="10" Click="AddRestrictedAppButton_Click"/>
                </StackPanel>

                <StackPanel Spacing="20">
                    <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" 
                            CornerRadius="12" Padding="20" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE81D;" Foreground="{StaticResource GoogleYellow}"/>
                                <TextBlock Text="Gaze Masking &amp; PII" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <ToggleSwitch x:Name="AutoBlurPasswordsToggle" Header="Auto-blur password fields in memory" Toggled="PiiToggle_Toggled" />
                            <ToggleSwitch x:Name="ScrubCcToggle" Header="Scrub Credit Card numbers from logs" Toggled="PiiToggle_Toggled" />
                            <ToggleSwitch x:Name="HilPaymentsToggle" Header="Enable Human-in-the-loop for payments" Toggled="PiiToggle_Toggled" />
                            <ToggleSwitch x:Name="LogKeyboardToggle" Header="Log keyboard input during active tasks" Toggled="PiiToggle_Toggled" />
                        </StackPanel>
                    </Border>

                    <Border Background="#1AEE4335" CornerRadius="12" Padding="20" BorderThickness="1" BorderBrush="{StaticResource GoogleRed}">
                        <StackPanel Spacing="10">
                            <TextBlock Text="Danger Zone" FontWeight="Bold" Foreground="{StaticResource GoogleRed}"/>
                            <TextBlock Text="Irreversibly wipe the neural database and local skills." FontSize="11" Opacity="0.8"/>
                            <Button x:Name="PurgeButton" Content="PURGE ALL NEURAL DATA" HorizontalAlignment="Stretch" Background="{StaticResource GoogleRed}" Foreground="White" Margin="0,10,0,0" Click="OnPurgeDataClick"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>