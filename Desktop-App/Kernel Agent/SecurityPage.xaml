<Page
    x:Class="Kernel_Agent.SecurityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kernel_Agent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="30">
        <StackPanel Spacing="25" MaxWidth="900" HorizontalAlignment="Left">

            <StackPanel Spacing="8" Margin="0,0,0,16">
                <TextBlock Text="SECURITY &amp; PRIVACY VAULT" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}" CharacterSpacing="50"/>
                <TextBlock Text="Configure the ethical and operational boundaries of your agent." Foreground="{StaticResource TextMutedBrush}" FontSize="14"/>
            </StackPanel>

            <Grid ColumnDefinitions="*, *" ColumnSpacing="20">

                <StackPanel Spacing="20">
                    <Border Background="{StaticResource CardBackgroundBrush}" 
                            CornerRadius="{StaticResource LargeRadius}" Padding="20" BorderThickness="1" BorderBrush="{StaticResource BorderSubtleBrush}">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE72E;" Foreground="{StaticResource ErrorBrush}"/>
                                <TextBlock Text="Application Blacklist" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <TextBlock Text="The agent will immediately terminate its session if these windows become active." FontSize="12" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap"/>

                            <ListView SelectionMode="None" Background="Transparent">
                                <ListViewItem>
                                    <Grid ColumnDefinitions="*, Auto" Width="350">
                                        <TextBlock Text="Banking (Chrome/Edge Tabs)" VerticalAlignment="Center"/>
                                        <ToggleSwitch Grid.Column="1" IsOn="True" OnContent="Protected" OffContent="Open"/>
                                    </Grid>
                                </ListViewItem>
                                <ListViewItem>
                                    <Grid ColumnDefinitions="*, Auto" Width="350">
                                        <TextBlock Text="Password Managers" VerticalAlignment="Center"/>
                                        <ToggleSwitch Grid.Column="1" IsOn="True" OnContent="Protected" OffContent="Open"/>
                                    </Grid>
                                </ListViewItem>
                                <ListViewItem>
                                    <Grid ColumnDefinitions="*, Auto" Width="350">
                                        <TextBlock Text="Private Messaging (Slack/Discord)" VerticalAlignment="Center"/>
                                        <ToggleSwitch Grid.Column="1" IsOn="False" OnContent="Protected" OffContent="Open"/>
                                    </Grid>
                                </ListViewItem>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <Button Content="Add Restricted Application" HorizontalAlignment="Stretch" Padding="10"/>
                </StackPanel>

                <StackPanel Spacing="20">
                    <Border Background="{StaticResource CardBackgroundBrush}" 
                            CornerRadius="{StaticResource LargeRadius}" Padding="20" BorderThickness="1" BorderBrush="{StaticResource BorderSubtleBrush}">
                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE81D;" Foreground="{StaticResource WarningBrush}"/>
                                <TextBlock Text="Gaze Masking &amp; PII" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <ToggleSwitch Header="Auto-blur password fields in memory" IsOn="True" />
                            <ToggleSwitch Header="Scrub Credit Card numbers from logs" IsOn="True" />
                            <ToggleSwitch Header="Enable Human-in-the-loop for payments" IsOn="True" />
                            <ToggleSwitch Header="Log keyboard input during active tasks" IsOn="False" />
                        </StackPanel>
                    </Border>

                    <Border Background="#0DEF4444" CornerRadius="{StaticResource LargeRadius}" Padding="20" BorderThickness="1" BorderBrush="#33EF4444">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Danger Zone" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}"/>
                            <TextBlock Text="Irreversibly wipe the neural database and local skills." FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <Button Content="PURGE ALL NEURAL DATA" HorizontalAlignment="Stretch" Background="{StaticResource ErrorBrush}" Foreground="White" Margin="0,8,0,0" CornerRadius="{StaticResource SmallRadius}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>