<Page x:ConnectionId='1'
    x:Class="Kernel_Agent.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kernel_Agent"
    xmlns:services="using:Kernel_Agent.Services">

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="EPISODIC MEMORY TIMELINE" FontSize="12" FontWeight="Bold" Opacity="0.6" VerticalAlignment="Center"/>
            <AppBarButton x:ConnectionId='8' Icon="Refresh" Label="Refresh"                             ToolTipService.ToolTip="Refresh Timeline"/>
            <ProgressRing x:ConnectionId='9' x:Name="LoadingProgress" IsActive="False" Width="20" Height="20" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Timeline Feed -->
        <ListView x:ConnectionId='2' Grid.Row="1"                                       SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate                                       >
                    <Grid x:ConnectionId='4' Margin="0,8,0,8" Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Time & Type Icon -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,4,0,0">
                            <TextBlock x:ConnectionId='7'                               FontSize="12" Opacity="0.5" HorizontalAlignment="Center"/>
                            <!-- Icon placehoder logic could go here -->
                        </StackPanel>

                        <!-- Content -->
                        <StackPanel Grid.Column="1" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Border Background="#20FFFFFF" CornerRadius="4" Padding="6,2">
                                    <TextBlock x:ConnectionId='6'                      FontSize="10" FontWeight="Bold"                                    CharacterSpacing="50"/>
                                </Border>
                            </StackPanel>
                            
                            <TextBlock x:ConnectionId='5'                         TextWrapping="Wrap" FontSize="14"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,10,0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>

